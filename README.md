🔁 Palindrome Checker — Level 1 Task 5

This task introduces a simple Python function that checks whether a given string is a palindrome.
A palindrome reads the same forwards and backwards — like "madam" or "racecar".

🚀 Task Overview:

1)Accept input from the user

2)Clean the string (remove spaces + make lowercase)

3)Compare original vs. reversed string

4)Display whether it’s a palindrome

🧠 Solution Logic:

We normalize the string by removing spaces and converting everything to lowercase.
Then we simply check if:

text == text[::-1]

This ensures a minimal, clean approach.

📌 How to Run:

1)Save the file as palindrome_checker.py

2)Run it using:

python palindrome_checker.py

3)Enter any word or phrase

🔍 Key Takeaways:

1)Efficient string reversal

2)Handles spacing + case differences

3)Beginner-friendly logic and structure
